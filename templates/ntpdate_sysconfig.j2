{{ ansible_managed | comment }}

# Options for ntpdate
OPTIONS="{{ ntp_ntpdate_init_options }}"

# Set to 'yes' to sync hw clock after successful ntpdate
SYNC_HWCLOCK="{{ ntp_ntpdate_hwclock_sync }}"
